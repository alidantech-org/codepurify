import 'package:flutter_api_bridge/flutter_api_bridge.dart';

class {{ project.name.pascal.o }} {
  const {{ project.name.pascal.o }}._();

  static const String defaultBaseUrl = '{{ project.meta.default_base_url }}';

  static void initialize({
    String? baseUrl,
  }) {
    ApiClient.instance(baseUrl ?? defaultBaseUrl);
  }
}
