/**
 * ---------------------------------------------------------------------------
 * {{ project.meta.api_version }} Public Barrel
 * ---------------------------------------------------------------------------
 *
 * Export only client-safe generated modules from this API version.
 */

/* Do not export './api' here. It contains server-only runtime values and can break client component barrel imports. */
/* Do not export './actions' here. Server action files must only be imported intentionally from server/action boundaries. */
export * from './routes';
export * from './types';
