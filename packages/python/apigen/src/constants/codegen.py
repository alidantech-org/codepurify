"""Custom x-codegen metadata constants."""

X_CODEGEN = "x-codegen"

# Resource keys
RESOURCE = "resource"
NAME = "name"
PATH = "path"

# Schema kind keys
KIND = "kind"

# Schema kind values
KIND_PRIMITIVE = "primitive"
KIND_ENUM = "enum"
KIND_MODEL = "model"
KIND_DTO = "dto"

# Target keys
TARGET = "target"

# Attribute names
ATTR_RESOURCE = "resource"

# Query metadata keys
QUERY = "query"
FILTER = "filter"
OPERATORS = "operators"
SORT = "sort"
SELECT = "select"
SEARCH = "search"

# UI metadata keys
UI = "ui"
ENABLED = "enabled"
ROLE = "role"
INFERRED = "inferred"
INFERENCE_SOURCE = "inferenceSource"
INFERENCE_REASON = "inferenceReason"
