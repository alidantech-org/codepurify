{# @codepot.select enums.each #}
{%- if enum.description %}
/**
 * {{ enum.description }}
 */
{%- endif %}
export enum {{ enum.lang.class_name }} {
{%- for value in enum.values %}
  {{ value.lang.constant_name }} = {{ value.value | tojson }},
{%- endfor %}
}
