{# @codepot.select models.each #}
{%- for import in imports %}
{{ import.rendered }}
{%- endfor %}

{%- if model.description %}
/**
 * {{ model.description }}
 */
{%- endif %}
export interface {{ model.lang.class_name }}{%- if model.lang.extends %} extends {{ model.lang.extends }}{%- endif %} {
{%- for field in model.fields %}
  {%- if field.description %}/** {{ field.description }} */{%- endif %}
  {{ field.lang.field_name }}{%- if field.lang.optional %}?{%- endif %}: {{ field.lang.annotation }};
{%- endfor %}
}
