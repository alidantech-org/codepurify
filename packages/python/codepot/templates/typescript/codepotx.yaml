version: 1
name: typescript-basic

language:
  name: typescript
  extensions:
    - ts
    - tsx
    - json
    - md
    - txt

defaults:
  global_alias: shared
  global_folders: []

ignore:
  file: .codepotxIgnore
  patterns:
    - .DS_Store
    - __pycache__/
    - "*.tmp"
    - "*.bak"
    - "*.old"

templates:
  enums:
    select: enums.each
    folders:
      - enums
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: true

  composites:
    select: composites.each
    folders:
      - composites
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: true
    resolves:
      enums:
        $ref: "#/templates/enums/barrel"

  models:
    select: models.each
    folders: 
      - models
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: true
    resolves:
      enums:
        $ref: "#/templates/enums/barrel"
      composites:
        $ref: "#/templates/composites/barrel"
      models:
        $ref: "#/templates/models/barrel"

  dtos:
    select: dtos.by_owner
    folders:
      - dtos
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: true
    resolves:
      enums:
        $ref: "#/templates/enums/barrel"
      composites:
        $ref: "#/templates/composites/barrel"
      models:
        $ref: "#/templates/models/barrel"

  errors:
    select: errors.each
    folders:
      - errors
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: true

  resources:
    select: resources.each
    folders:
      - resources
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: false
    resolves:
      route_paths:
        $ref: "#/templates/route_paths"
      routes:
        $ref: "#/templates/routes"
      operations:
        $ref: "#/templates/operations"

  route_paths:
    select: route_paths.by_owner
    folders:
      - resources
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: false

  routes:
    select: routes.by_owner
    folders:
      - resources
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: false

  operations:
    select: operations.by_owner
    folders:
      - resources
      - "[owner.folders]"
      - "[owner.name.path]"
    barrel: false