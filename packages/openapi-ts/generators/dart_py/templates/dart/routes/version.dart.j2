{%- for import_uri in plan.imports %}
import '{{ import_uri }}';
{%- endfor %}

class {{ plan.class_name }} {
  {{ plan.class_name }}._();

{%- for group in plan.endpoint_groups %}
  static const {{ group.property_name }} = {{ group.class_name }}();
{%- endfor %}
}
