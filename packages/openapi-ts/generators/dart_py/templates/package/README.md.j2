# {{ metadata.title }}

{{ metadata.description | default('Generated Dart SDK for ' + metadata.title, true) }}

## Version

{{ metadata.version }}

## Installation

Add this package to your `pubspec.yaml`:

```yaml
dependencies:
  {{ metadata.name }}:
    path: ../{{ metadata.name }}
```

Or install from git:

```bash
dart pub add {{ metadata.name }} --git-url=https://github.com/example/{{ metadata.name }}.git
```

## Setup

This package uses `flutter_api_bridge` for HTTP, auth, caching, cookies, uploads, and typed API results.

Initialize the bridge before using generated features:

```dart
import 'package:flutter_api_bridge/flutter_api_bridge.dart';

await Server.init(
  baseUrl: '{% if metadata.servers %}{{ metadata.servers[0] }}{% else %}https://api.example.com{% endif %}',
  authStrategy: BearerStrategy(tokenKey: 'access_token'),
);
```

## Usage

```dart
import 'package:{{ metadata.name }}/{{ paths.version_folder }}/{{ metadata.name }}.dart';
```

## Servers

{% for server in metadata.servers %}
- {{ server }}
{% endfor %}

## Tags

{% for tag in metadata.tags %}
- {{ tag }}
{% endfor %}

## License

MIT
