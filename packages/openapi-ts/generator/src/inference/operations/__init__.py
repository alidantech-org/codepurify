"""
Operation inference package.

This package infers operations from OpenAPI paths, connecting them to parameters,
request bodies, responses, content types, and reachable schema refs. Operations are
treated as connected workflows unlike standalone schemas.
"""
