"""
Rich table presentation package.

This package contains presenter functions that render CLI output using Rich tables.
Each presenter handles the display for a specific command: inspection summary,
inference summary, emission summary, and validation results.
"""
