{%- for resource in resources %}
import 'constants/{{ resource.name.path | string }}_endpoints.dart';
{%- endfor %}

class V1Routes {
  const V1Routes._();

{%- for resource in resources %}
  static const {{ resource.name.camel | string }} = {{ resource.name.pascal | string }}Endpoints();
{%- endfor %}
}
