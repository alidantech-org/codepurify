/**
 * MEDIA SCHEMA ENUMS
 */
export enum EMediaTable {
  TaxonomyMedia = 'taxonomy_media',
  BusinessMedia = 'business_media',
}

/**
 * Supported media types
 */
export enum EMediaType {
  Image = 'image',
  Icon = 'icon',
  Video = 'video',
}

/**
 * Media usage context
 */
export enum EMediaUsage {
  Thumbnail = 'thumbnail',
  Banner = 'banner',
  Gallery = 'gallery',
}
