/**
 * CONTENT SCHEMA ENUMS
 */
export enum EContentTable {
  UiKeys = 'ui_keys',
  UiTranslations = 'ui_translations',
  UiContextLabels = 'ui_context_labels',
}

/**
 * UI label usage type
 */
export enum EUILabelType {
  Generic = 'generic',
  Contextual = 'contextual',
  BusinessSpecific = 'business_specific',
}
