services:
  site:
    build:
      context: ./site
      dockerfile: Dockerfile
    ports:
      - "3020:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./site:/app
      - /app/node_modules
    restart: unless-stopped
    container_name: codepurify-site
