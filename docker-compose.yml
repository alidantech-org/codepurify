services:
  www:
    build:
      context: ./www
      dockerfile: Dockerfile
    ports:
      - "3020:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./www:/app
      - /app/node_modules
    restart: unless-stopped
    container_name: codepurify-www
