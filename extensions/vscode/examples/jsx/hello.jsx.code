{!# JSX example #!}

function {!entity.name.pascal!}() {
  return (
    <div>
      <h1>{!entity.name.pascal!}</h1>
      {!loop field in entity.fields!}
      <p>{!field.name.camel!}: {!field.type!}</p>
      {!/loop!}
    </div>
  );
}

export default {!entity.name.pascal!};
